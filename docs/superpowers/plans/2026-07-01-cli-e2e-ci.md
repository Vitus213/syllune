# CLI E2E CI Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Make `type4me-linux` verifiable as a command-line Linux voice input backend with unit, integration, and end-to-end tests, 90% coverage, pre-commit, and GitHub Actions CI.

**Architecture:** Keep production code small and dependency-free. Tests simulate external Linux tools with executable fixtures so CI can validate the real CLI orchestration without microphone, Wayland, GPU, or downloaded ASR models.

**Tech Stack:** Python 3.12, pytest, pytest-cov, ruff, pre-commit, Nix flake, GitHub Actions.

---

### Task 1: Testable CLI and Daemon Surfaces

**Files:**
- Modify: `src/type4me_linux/daemon.py`
- Modify: `src/type4me_linux/cli.py`
- Test: `tests/test_daemon.py`
- Test: `tests/test_cli.py`

- [x] Write failing tests for daemon `/health`, `/inject`, and `/transcribe`.
- [x] Add a `make_server(config)` helper so tests can run the daemon on port `0`.
- [x] Write CLI tests for JSON transcription and record-without-injection using fake backend.
- [x] Run targeted tests and make them pass.

### Task 2: Integration and E2E Tests With Fake Commands

**Files:**
- Create: `tests/conftest.py`
- Create: `tests/test_integration_cli.py`
- Create: `tests/test_e2e_cli.py`
- Modify: `src/type4me_linux/providers.py`
- Modify: `src/type4me_linux/capture.py`

- [x] Add executable fake-command fixtures for `pw-record`, `sherpa-onnx-offline`, `wtype`, and `wl-copy`.
- [x] Write failing integration tests for `doctor --allow-missing-models` and `transcribe --backend sensevoice --inject`.
- [x] Write failing E2E test for `record --seconds ...` through fake recorder, fake ASR, snippets, and fake injection.
- [x] Add only the small production changes needed for robust command invocation and error messages.
- [x] Run targeted integration/E2E tests and make them pass.

### Task 3: Coverage, Pre-commit, CI, and Documentation

**Files:**
- Modify: `pyproject.toml`
- Modify: `flake.nix`
- Modify: `Justfile`
- Create: `.pre-commit-config.yaml`
- Create: `.github/workflows/ci.yml`
- Modify: `README.md`

- [x] Add pytest markers and `pytest-cov` coverage gate at 90%.
- [x] Add ruff and pre-commit config.
- [x] Add Nix dev shell/check inputs for pytest-cov and pre-commit.
- [x] Add GitHub Actions workflow running tests, coverage, and `nix flake check`.
- [x] Document test commands, CI, and fake E2E scope.
- [x] Run formatting, pre-commit, coverage tests, and flake check.
- [ ] Commit and push to GitHub remote.
